name = "purecleaning-api"
main = "src/index.js"
compatibility_date = "2026-05-01"
account_id = "ffe39f1ca6f7d9685b881c704c7ae51a"

[[kv_namespaces]]
binding = "DATA"
id = "76a0da4d65264885b06ed1f1aa25e27d"

[vars]
ENVIRONMENT = "production"

# ── Phase 2: Bouncie GPS cron trigger (activate when hardware arrives) ──
# Uncomment and deploy after Bouncie credentials are added as Worker secrets:
#
# [[triggers.crons]]
# cron = "* * * * *"   # every 60 seconds — fetch truck positions from Bouncie
#
# Required secrets to add via `wrangler secret put`:
#   BOUNCIE_CLIENT_ID
#   BOUNCIE_CLIENT_SECRET
#   BOUNCIE_REFRESH_TOKEN
