# Dependencies
node_modules/

# Build output
build/
dist/

# Environment / secrets
.env
.env.local
.env.*.local

# Wrangler local state
.wrangler/
.dev.vars*
!.dev.vars.example

# Logs
*.log
npm-debug.log*

# macOS
.DS_Store

# Temp KV backups
customer_db_backup_*.json

# Test coverage
coverage/
verify-screenshots/

# customer-PII migration inputs — never commit
/outputs/

# Transient / scratch — kept on disk, out of the repo (2026-06-23 tree cleanup)
/__deltest
*.pptx
/claude-code-handoffs/
scripts/apply_*.py
scripts/verify_cleanup_spec.py

# Design mockups — scratch previews from the mockup-first workflow (not deployed)
/mockups/

# Raw photo staging — candidate originals / drive dumps, NOT site assets.
# NOTE: gitignore ≠ backup. Archive these originals to R2 / cloud separately.
/website-assets/

# Stray unreferenced test images (no page links them)
/public/images/a.png
/public/images/b.png
/public/images/c.png
